import styles from "./Topblur.module.css";

export const Topblur = () => { // Ensure the component is named correctly
    return (
      <div className={styles.topBlur}>
        
      </div>
    );
  };
  
